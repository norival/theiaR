# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
sudo: false
cache: packages

branches:
  only:
    - devel
    - master

after_success:
  - Rscript -e 'covr::codecov()'

r_github_packages:
  - appelmar/gdalcubes_R
